#include"stdio.h"
#include"math.h"
int main()
{
/*
1.Örnek
	int a = 89;
	float b = 12.56, ort;
	ort = (a + b) / 2;
	
	printf("%lf", ort);
*/
	
/*
2.Örnek
	double kenar, h, alan;
	
	printf("Ucgenin kenarini giriniz: ");
	scanf("%lf",& kenar);
	
	printf("Ucgenin yuksekligini giriniz: ");
	scanf("%lf",& h);
	
	alan = kenar * h / 2;
	
	printf("%lf", alan);
*/

/*
3.Örnek
	double vize, final, ort;
	
	printf("Vize notunu giriniz: ");
	scanf("%lf",& vize);
	
	printf("Final notunu giriniz: ");
	scanf("%lf",& final);
	
	ort = (vize + final) / 2;
	
	printf("%lf", ort);
*/

/*
4.Örnek
	double yaricap, cevre, alan, pi = 3.14;
	
	printf("Yaricapi giriniz: ");
	scanf("%lf",& yaricap);
	
	cevre = 2 * pi * yaricap;
	alan = pi * yaricap * yaricap;
	
	printf("Cevre: %lf\n", cevre);
	printf("Alan: %lf", alan);
*/

/*
5.Örnek
	double aci, pi = 3.14, grand, radyan;
	
	printf("Aciyi giriniz: ");
	scanf("%lf",& aci);
	
	grand = aci * pi / 200;
	radyan = aci * pi / 180;
	
	printf("Radyan: %lf\n", radyan);
	printf("Grand: %lf", grand);
*/

/*
6.Örnek
	int k_kenar, u_kenar, cevre, alan;
	
	printf("Kisa kenari giriniz: ");
	scanf("%d",& k_kenar);
	
	printf("Uzun kenari giriniz: ");
	scanf("%d",& u_kenar);
	
	cevre = 2 * (k_kenar + u_kenar);
	alan = k_kenar * u_kenar;
	
	printf("Cevre: %d\n", cevre);
	printf("Alan: %d", alan);
*/

/*
7.Örnek
	float fiyat, kdvli_fiyat, kdv;
	
	printf("Fiyati giriniz: ");
	scanf("%f",& fiyat);
	
	kdv = fiyat * 18 / 100;
	kdvli_fiyat = fiyat + kdv;
	
	printf("Urunun yeni fiyati: %f", kdvli_fiyat);
*/
	
/*
8.Örnek
	float kenar1, kenar2, hipo = 0;
	
	printf("Birinci kenari giriniz: ");
	scanf("%f",& kenar1);
	
	printf("Ikinci kenari giriniz: ");
	scanf("%f",& kenar2);
	
	hipo = sqrt(((kenar1 * kenar1) + (kenar2 *kenar2))) ;
	
	printf("Hipotenus: %f", hipo);
*/

/*
9.Örnek
	double yaricap, hacim, alan, pi = 3.14;
	
	printf("Yaricapi giriniz: ");
	scanf("%lf",& yaricap);
	
	alan = (4 * pi * pow(yaricap, 2)) / 3;
	hacim = (4 * pi * pow(yaricap, 3)) / 3;
	
	printf("Alan: %lf\n", alan);
	printf("Hacim: %lf", hacim);
*/

	
}
